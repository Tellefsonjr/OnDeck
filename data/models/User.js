class User {
    constructor(id, email, password, profile, preferences){
      this.id = id;
      this.email = email;
      this.password = password;
      this.profile = profile;
      this.preferences = preferences;
    }
  }
  
  export default User;
  